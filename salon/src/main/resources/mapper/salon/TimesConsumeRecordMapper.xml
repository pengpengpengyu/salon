<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.salon.mapper.TimesConsumeRecordMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.ruoyi.salon.domain.entity.TimesConsumeRecord">
        <id column="times_consume_record_id" property="timesConsumeRecordId" />
        <result column="member_id" property="memberId" />
        <result column="item_id" property="itemId" />
        <result column="item_name" property="itemName" />
        <result column="consume_times" property="consumeTimes" />
        <result column="times_original" property="timesOriginal" />
        <result column="times_after" property="timesAfter" />
        <result column="consume_date" property="consumeDate" />
        <result column="operator" property="operator" />
        <result column="remark" property="remark" />
        <result column="del_flag" property="delFlag" />
        <result column="create_by" property="createBy" />
        <result column="create_time" property="createTime" />
        <result column="update_by" property="updateBy" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        times_consume_record_id, member_id, item_id, item_name, consume_times, times_original, times_after, consume_date, operator, remark, del_flag, create_by, create_time, update_by, update_time
    </sql>

    <select id="selectVoList" resultType="com.ruoyi.salon.domain.vo.TimesConsumeRecordVo">
        select t.times_consume_record_id  , t.member_id, t.item_id,t.consume_times ,
               t.times_original  ,t.times_after ,
               t.consume_date ,t.operator,h.member_name, h.mobile_no,if(i.item_name != null and i.item_name != '', i.item_name, t.item_name) as item_name
        from t_times_consume_record t
                 left join t_member h on t.member_id = h.member_id and t.del_flag = '0' and h.del_flag = '0'
                 left join t_item i on t.item_id = i.item_id and t.del_flag = '0' and i.del_flag = '0'
        where
            t.del_flag = '0'
          and h.del_flag = '0'
        <if test="memberId != null">
            and t.member_id = #{memberId}
        </if>
        <if test="itemId != null">
            and t.item_id = #{itemId}
        </if>
        <if test="mobileNo != null and mobileNo != ''">
            and h.mobile_no = #{mobileNo}
        </if>
        <if test="memberName != null and memberName != ''">
            and h.member_name like concat(#{memberName}, '%')
        </if>
        <if test="beginTime != null and endTime != null">
            and t.consume_date  between #{beginTIme} and #{endTime}
        </if>
        order by t.times_consume_record_id desc
    </select>

</mapper>
